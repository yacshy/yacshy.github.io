import { Element } from 'hast';
export declare function getChilds(data: Element[], level: number, result?: Element[]): Element[];
interface Data extends Element {
    number?: number;
}
/** 获取 Heading 到下一个 Heading 之间的内容*/
export declare function getHeader(data: Data[], level: number, result?: Data[]): Data[];
export {};
