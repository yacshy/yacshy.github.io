import type { IFileDirStat } from 'recursive-readdir-files';
import { ParsedArgs } from 'minimist';
export declare const __filename: string;
/**
 * 搜索数据路径
 * */
export declare const SEARCH_DATA_CACHE: string;
/**
 * 把附件 copy 到打包目录
 */
export declare function copyAttachment(options: Options): Promise<void>;
/**
 * 把源码中的 style 复制到打包结果目录中的 style
 */
export declare function copyCSSFile(options: Options): Promise<void>;
/**
 * 把源码中的 js 复制到打包结果目录中的 js
 */
export declare function copyJSFile(options: Options): Promise<void>;
export interface Config {
    title?: string;
    description?: string;
    keywords?: string;
    footer?: string;
    license?: string;
    giscus?: Record<string, string>;
    'data-info'?: string;
    search?: {
        label: string;
        placeholder: string;
        cancel: string;
    };
    editor?: {
        label: string;
        disabled?: boolean;
    };
    github?: {
        url: string;
    };
    home?: {
        label: string;
        url: string;
    };
    inputDir?: string;
    inputPath?: string;
    outputDir?: string;
    outputPath?: string;
    attachmentDir?: string;
    attachmentPath?: string;
    server?: {
        port: number;
        root: string;
        file: string;
    };
}
export interface Options extends Partial<ParsedArgs> {
    static_path?: string;
    watch?: boolean;
    build?: boolean;
    force?: boolean;
    config?: Config;
}
export declare function run(options: Options): Promise<void>;
/**
 * 递归函数，根据 files[num] 创建html
 * @param files
 * @param opts
 * @param num number 自增索引
 * @returns
 */
export declare function createHTML(files: IFileDirStat[], opts: Options, num?: number): Promise<void>;
export declare const pkg: any;
export declare const helpStr: string;
