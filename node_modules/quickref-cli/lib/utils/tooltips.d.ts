import { Root, Element, RootContent } from 'hast';
/**
 * 配置 tooltips 注释
 *
 * ```markdown
 * - [超链接有 tooltips 提示](#1xx-information) _Tooltips 展示内容_ <!--rehype:tooltips-->
 * ```
 *
 * 上面示例：将 “Tooltips 展示内容” 放到 前一个 `<a>` dom 节点作为子节点
 *
 * - 注释配置的，前一个节点 A，A 的前一个节点 B
 * - 如果 A 和 B 其中一个不存在 `tooltips` 将失效
 * - 设置 B 的类名称为 tooltips
 */
export declare function tooltips(node: Root | RootContent, index: number, parent: Root | Element): void;
export declare function getPreviewNode(): void;
