import { Root, Element, RootContent } from 'hast';
import { DetailData } from './create.js';
import { Options as RunOptions } from '../utils/utils.js';
export declare function homeCardIcons(node: Root | RootContent, parent: Root | Element, { isHome, static_path, config }: RunOptions): Record<string, DetailData>;
