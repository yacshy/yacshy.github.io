import { Option as InitOption } from './search.js';
import { Element, ElementContent } from 'hast';
interface Option extends InitOption {
    githubURL?: string;
}
export declare function getReferrals(options?: Option): MenuData[];
type MenuData = {
    menu?: boolean;
    href?: string;
    target?: string;
    label?: string;
    class?: string[];
    id?: string;
    children?: (Element | ElementContent)[];
};
export declare function header(options?: Option): Element;
export {};
