import { Element } from 'hast';
import { Option as InitOption } from './search.js';
interface Option extends InitOption {
    githubURL?: string;
}
export declare function darkMode({ homePath, isHome, static_path }?: Option): Element[];
export {};
